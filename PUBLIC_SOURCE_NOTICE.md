# OmKwam 0.34d Public GitHub Package

This package redacts private prompt and default-dictionary text from `src/app.js`.

It is intended for public source review and repository publication. It is not the complete functional deployment package unless the maintainer restores the private prompt text.

For a working static deployment, use `omkwam-0.34d-linux-deploy.zip`.
